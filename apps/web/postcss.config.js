/**
 * PostCSS config for the whiteboard web app.
 *
 * Runs Tailwind CSS v3 and Autoprefixer as PostCSS plugins.
 * Used by Vite's built-in CSS pipeline.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
