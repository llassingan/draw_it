// Global Vitest test-setup file.
// Imports jest-dom matchers (toBeInTheDocument, toHaveTextContent, …)
// so they are available in every test without per-file imports.

import '@testing-library/jest-dom/vitest';
